# Project agents

Custom Cursor agents for Justin Bot. Files here appear in the **Agents** sidebar and should be committed to git.

| Agent | Focus |
|-------|--------|
| `justin-bot-core` | Python Justin Bot package (`justin-bot/`) |
| `pro4-mealdeals` | Sibling `MealDeals/` website |

Parent workspace: `~/Projects/JustinBot/` (Justin Bot header with MealDeals + new site folders).

See `.cursor/skills/agents-in-sidebar/SKILL.md` for the required workflow.
